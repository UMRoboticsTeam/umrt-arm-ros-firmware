controller_manager:
  ros__parameters:
    update_rate: 10  # Hz

    can_ppm_controller:
      type: forward_command_controller/ForwardCommandController

can_ppm_controller:
  ros__parameters:
    joints:
      - gripper_servo
      - gripper_solenoid

    interface_name: position
